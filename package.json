{
  "name": "crash-dump",
  "version": "2.0.1",
  "description": "Create a mini dump when the application crashes.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Nexus-Mods/node-crash-dump.git"
  },
  "scripts": {
    "install": "prebuild-install.cmd -r napi -t 4 -a x64 || prebuild-install -r napi -t 4 -a x64",
    "developer": "cmake-js.cmd compile || cmake-js compile",
    "devbuild": "prebuild.cmd -r napi -t 4 -a x64 --backend cmake-js || prebuild -r napi -t 4 -a x64 --backend cmake-js",
    "prebuild": "prebuild.cmd -r napi -t 4 -a x64 --prepack codesign --backend cmake-js || prebuild -r napi -t 4 -a x64 --prepack codesign --backend cmake-js"
  },
  "binary": {
    "napi_versions": [
      4
    ]
  },
  "author": "Black Tree Gaming Ltd",
  "license": "GPL-3.0",
  "devDependencies": {
    "cmake-js": "^6.3.2",
    "node-addon-api": "^5.0.0",
    "prebuild": "^11.0.3"
  },
  "dependencies": {
    "prebuild-install": "7.1.0"
  }
}
